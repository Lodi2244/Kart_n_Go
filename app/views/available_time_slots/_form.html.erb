<%= form_with(model: available_time_slot, local: true) do |form| %>
  <% if available_time_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(available_time_slot.errors.count, "error") %> prohibited this available_time_slot from being saved:</h2>

      <ul>
      <% available_time_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hour %>
    <%= form.datetime_select :hour %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
